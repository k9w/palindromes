using System;
using Palindrome;
namespace Palindrome
{
  public class IsThisWordAPalindrome
  {
    
    /*

    Input word from the user.

    Split it into single letters and assign to an array.

    Reverse the array.

    Check if it's still an Engllish word.

    Output to the user the result and tell them if it's a word.


    */
    public void CheckPalindrome()
    {
      // reverseWord(inputWord)
      // {
      //   string[] = inputWord.Split('');
      //   Array.Reverse(string[]);
       
      // }
       string s = "robot";
        char[] charArray = s.ToCharArray();
        Console.WriteLine(charArray);

      // if (reverseWord(inputWord)))
      // {
      //   return true;
      // }
      // else
      // {
      //   return false;
      // }
    }

  }
  
}
